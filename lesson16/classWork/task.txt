1. Реализовать на ЯП го веб-сервер, с 2 URI:
    1) /auth - принимает GET-запрос, и возвращает token = "12345"
    2) /secret - при обращении методом get проверяет в request заголовок "Token". Он должен соответствовать тому, что выдает /auth. Если не совпадает - возвращает FAIL. В плоожительном случае - вернет строку "SECRET"
2. Клиента под это дело